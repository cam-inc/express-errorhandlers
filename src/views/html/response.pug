#response
  h1 Response
  div
    span.key Status
    pre.
      #{data.status}
  div
    span.key Message
    pre.
      #{data.message}
  div
    span.key Extra
    pre.
      #{JSON.stringify(data.extra, null, '  ') || '-'}
